# Audit tracker

One row per round; the round's own `TRACKER.md` holds a row per finding. A round's disposition is `**RESOLVED**` once
it is filed under `audits/implemented/`, otherwise `OPEN`.

| Disposition | Round | Findings | Where |
|-------------|-------|----------|-------|
